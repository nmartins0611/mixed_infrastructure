[webservers]

windows_demo.prometheus.io ansible_connection=winrm ansible_user= ansible_password= ansible_winrm_server_cert_validation=ignore

webserver.prometheus.io

# [webservers:vars]
# ansible_connection=winrm
# ansible_user=
# ansible_password=
# ansible_winrm_server_cert_validation=ignore
